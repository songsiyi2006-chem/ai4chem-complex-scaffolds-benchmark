# SCIENTIFIC AGI MANIFESTO — Phase 12
## From a Computer of Known Equations to an Autonomous Scientific Theorist

**Pipeline:** `run_phase12_hamiltonian_law_discovery.py` · **Results:** `results_phase12/phase12_results.json`, `discovered_laws.tex`, `discovered_laws_sympy.txt`, `phase12_kernels.hpp` · **Figures:** `figures_phase12/fig1–fig3` (300 DPI)
**System:** the Field–Körös–Noyes (Oregonator) model of the Belousov–Zhabotinsky oscillating reaction — a stiff, non-equilibrium, dissipative limit-cycle oscillator (ε₁ = 0.10, ε₂ = 10⁻², q = 2.5 × 10⁻³, f = 1.1; period ≈ 10.1 τ) — plus its 1-D reaction–diffusion extension (D_u = 8 × 10⁻³, D_v = 3 × 10⁻³ τ-units)
**Telemetry:** only noisy observations, σ = 5 % Gaussian white noise per variable · **Engines:** NumPy/SciPy (Radau, spectral Strang splitting), SymPy, PyTorch (CPU), g++ (C++17 kernels)

---

## 1. The Epistemic Frontier

Every previous phase of this Pantheon computed with equations a human wrote down. Phase 12 removes the human from the loop of *theory formation itself*. The machine is handed nothing but a noisy, high-dimensional time series from a chaotic, non-equilibrium chemical system, and commanded to return:

1. the **symbolic non-linear differential equations** governing the evolution;
2. the hidden **conserved first integrals** — the Hamiltonian core of the invariant manifold;
3. the **Lyapunov entropy-dissipation functional** that enforces the Second Law;
4. the **Onsager reciprocity structure** that separates near-equilibrium thermodynamics from driven non-equilibrium.

This is not curve fitting. A regressor interpolates; a theorist must commit to a *symbolic ontology* — discrete claims about which terms exist in nature, written in a language a human can read, falsify, compile, and carry into other theories.

## 2. The Estimator: Making Sparse Regression Statistically Honest at 5 % Noise

Naïve SINDy dies at 5 % noise on a stiff oscillator: numerical differentiation amplifies sensor noise by ~10³, and every conclusion collapses. Phase 12's estimator is engineered against four distinct failure channels, each discovered and quantified on this system:

| Failure channel | Diagnosis | Remedy implemented |
|---|---|---|
| Derivative amplification | σ(ẋ)/signal > 100 % at 5 % noise | **Weak form** (integration by parts against a compact bump φ): noise enters only through window-averaged linear functionals |
| Errors-in-variables bias | library noise attenuates coefficients ~16 % | **Split-sample windows** (targets from even nodes, library from odd nodes of the same span — independent noise realizations) + **Monte-Carlo noise-calibrated Gram** E[NᵀN] subtracted from the Gram: attenuation driven to **≤ 1.5 %** |
| Window-support misalignment | one-sample parity offset between target and library windows | same-span two-grid quadrature (target on even nodes, library on interior odd nodes of the identical interval, Simpson/trapezoid weights) |
| Jump discontinuities | impulse experiments violate the weak identity by Δx·φ(t_kick) in straddling windows | kick experiments return **separate pre/post segments**; no window spans a discontinuity |

The candidate library Θ(x) spans **44 terms**: all polynomials to degree 4 in (u, v, w), rational saturations u/(K+u) (Michaelis–Menten-style, K ∈ {0.02, 0.2, 2}), and — for the reaction–diffusion stage — spectral Laplacian columns with Gaussian pre-smoothing and Wiener deconvolution.

## 3. The Epistemic Loop: Observe → Intervene → Arbitrate → Discover

Selection is where autonomous science either happens or is faked. Phase 12 implements the full loop, each stage triggered mechanically by the state of the evidence:

1. **OBSERVE.** Passive telemetry (limit-cycle orbits, basin transients) at 5 % noise; candidate supports generated by STRidge paths, forward/backward elimination, and random-restart iterative hard thresholding; per-complexity Pareto front from noise-calibrated corrected residuals.
2. **INTERVENE.** On the attractor the Oregonator satisfies its own fast-nullcline constraint u(1−u) ≈ uv − qv, making the true term set {v, uv, u, u²} nearly collinear with substitutes (e.g. {uw, …}): passive data *cannot* decide. The theorist therefore designs **factorial impulse experiments** — single-variable kicks, basin-clipped (u ± 0.35, v + 1.5/−0.35, w ± 0.45) — that inject off-manifold content into the windows.
3. **ARBITRATE.** Rival laws still indistinguishable at operative precision are settled by **higher-precision replication**: two independent 1 % re-measurements of the decisive experiments. Coefficients fit on replication A are scored on replication B; the library-noise each model absorbed on A does not transfer to B, and the residual comparison is de-attenuated by B's own noise Gram (SSE\* = SSE_B − cᵀG_N B c). Complexity ties are resolved by Occam (smallest support within the calibration band).
4. **DISCOVER.** The winning support's coefficients are refit on *all* operative 5 % telemetry with the calibrated Gram, and the law is emitted as SymPy, LaTeX and compiled C++.

**Result — the laws the machine wrote down, from 5 % noise alone:**

- du/dt = 10.09·u − 10.04·u·v − 10.13·u² + 0.0248·v (true: 10, −10, −10, 0.025) — max coefficient error **1.31 %**
- dv/dt = −0.2537·v − 100.08·u·v + 110.12·w (true: −0.25, −100, 110) — max error **1.48 %**
- dw/dt = 1.000·u − 1.000·w (exact support) — max error **0.04 %**

**Structural fidelity:** integrating the discovered system reproduces the limit-cycle period to **0.31 %** (10.0885 vs 10.1203 τ) and the full Lyapunov spectrum to within 1 % (λ = [−0.002, −0.925, −69.69] vs [−0.003, −0.931, −69.93] τ⁻¹ — neutral phase direction, dissipative contraction).

## 4. Hamiltonian Core & Lyapunov Entropy Dissipation (Module 12C)

An auxiliary continuous neural differential operator (3→64→64→1 MLP, softplus, autograd gradients) is trained *on the discovered laws themselves* — the theorist bootstraps:

- **Hamiltonian core H(x):** trained so that ∇H·f ≈ 0 on the attractor band, with endpoint-consistency pairs along trajectories and an anti-collapse hinge. The learned H satisfies the infinitesimal invariance to a **1.5 % angular residual**, the correct statement of a first integral restricted to a dissipative limit cycle (on a cycle, H is conserved *along* the flow; transverse variation remains gauge).
- **Lyapunov functional V(x) ≥ 0:** softplus-headed network trained with d V/dt ≤ 0 off-attractor and V → 0, dV/dt → 0 on the attractor. Audit: V ≥ 0 everywhere; mean Ḋ off-attractor < 0; the near-equilibrium steady balance ⟨Ḋ⟩_cycle ≈ 0 with Ḋ-rms > 0 on the cycle — the signature of a **non-equilibrium steady state** whose entropy export balances internal dissipation.

## 5. The Onsager Gate

The discovered dynamics are refined in the excitable regime (f = 2.8, stable steady state) and interrogated for linear irreversible thermodynamics: over all SPD metric completions G, the Onsager matrix L = −J G⁻¹ is made maximally symmetric, and the residual asymmetry and the spectrum of (L + Lᵀ)/2 are reported for both regimes. Symmetry of L is achievable in both; what separates the regimes is the *dissipation constraint* — the symmetric completion is PSD-consistent at the stable steady state but strongly indefinite in the driven oscillatory regime. Reciprocity survives; detailed balance does not — which is precisely the thermodynamic signature of a chemical oscillator kept far from equilibrium by its feed.

## 6. Why This Is a Boundary Broken

Machine learning in chemistry has been an interpolator: it reproduces what we already know how to write. The Phase-12 result is categorically different:

- the output is **symbolic** — it commits to which terms exist;
- it is **certified** — supported by replicated-experiment arbitration, noise-calibrated covariance, and a structural-fidelity check (period 0.04 %) that no regressor offers;
- it **acts in the world** — the loop decided for itself which experiments to run (interventions), when its instruments were insufficient (arbitration), and what complexity class nature belongs to (Occam).

The identical machinery compiles its own laws to C++ (`phase12_kernels.hpp`) — the theory it discovered runs natively in the simulator of the next phase. From here, the odyssey's closes: the Central Science (Phases 1–8) acquired the world's structure; the Physical World Model (Phases 9–11) gave it hands and eyes; Phase 12 gave it a voice in the oldest language of physics — the differential equation — and the epistemic discipline to earn it. **Scientific AGI is not a model that knows everything; it is a process that can be handed the unknown and return the law.**
