# 新增计算工具与数据：实际适用性核验

核验日期：2026-09-11。本记录不把软件安装成功当作科学方法验证，
也不将公开数据或模拟结果称为本项目的实验测量。

## 已实际用于解决问题

**geomeTRIC 1.1.1 → Phase4**。本机 chem-ai4s 扩展环境可导入，
使用 Cartesian TS 优化器连接现有 GFN2-xTB 能量/梯度，不更换势能面。
实现 eV、Å 与 Hartree、Bohr 的显式换算，并通过解析梯度差分单测。
参见 [官方 TS 文档](https://geometric.readthedocs.io/en/latest/transition.html)。

- 原 NEB 最大残余力为0.15066261 eV/Å，仍未达到0.05，历史结果不覆盖。
- 首次50步有界尝试未收敛；第二次上限200步，在第62步收敛。
- 随后的独立 xTB 振动分析暴露 Windows 默认编码读取 UTF-8 cm⁻¹ 文本的错误。
  已修复振动文件/模式读取，并使用闭壳层明确参数 `--uhf 0`。
- 仅对已保存的收敛几何重新算梯度/Hessian，没有重复优化。
  最大力0.0029317686 eV/Å，唯一显著虚频−147.28 cm⁻¹（阈值−20 cm⁻¹）。
- 这证明当前近似方法下的驻点/单虚频检查通过；**尚未进行 IRC，不能据此确认
  连接指定反应物/产物、反应速率或实验机制**。坐标、源码快照、日志与哈希单独保留。

运行辅助程序 `refine_phase4_geometric.py <原尝试目录>`；
`--verify-from <已收敛精修目录>` 可单独复核，但必须通过原路径哈希、
收敛日志和原子身份检查。此工具依赖已安装的 ASE、NumPy、geomeTRIC 和 xTB，
不是无依赖的独立发行版。

## 已确认可用，但不直接替换当前计算

| 资源 | 本地证据与适用范围 | 当前限制 |
|---|---|---|
| BioPython 1.88 / MDAnalysis | BioPython 实读1UBQ mmCIF，共660原子，计入异质残基/水后134个残基条目；适合结构/轨迹检查 | 单个示例不是训练语料，也不能自动修好设计酶 |
| BoTorch / GPyTorch / Optuna / pymoo | 安装记录可见；适合后续筛选、贝叶斯优化与多目标搜索 | 本轮未将其替换进正在运行的算法 |
| MACE-MH-1、MACE-POLAR-1、MACE-OFF23-small、OrbMol-v2 | 本地权重存在，文件长度与安装记录一致；安装验证含水/硅小体系 | 需验证目标元素、带电/自旋态及反应势能面；不可用小体系冒烟测试证明目标适用性 |
| QM9 | 本地CSV 133,885条；实际读取首条分子及轨道字段成功 | 当前是属性表，不含对应完整3D坐标归档；不能直接补 TS |
| MoleculeNet 子集 | ESOL、FreeSolv、Lipophilicity、BBBP、BACE、Tox21、ClinTox、SIDER | 适合性质预测对照；不是当前反应路径/蛋白设计的真值 |
| Matbench 子集 | dielectric、expt_gap、steels、glass 可见于本地目录 | 材料性质任务，不直接用于分子 TS 或肽链闭环 |
| ORD / PDB 示例 | ORD 本地选定数据集750条；PDB 本地1UBQ示例 | 不是完整数据库镜像；公共 API 可访问不等于数据全已下载 |

硬件仍为 CPU、约16 GB RAM，无 CUDA。新增包不等于新增计算资源，
不额外启动大型模型训练或并行占满内存。UMA仍受模型访问限制；
本轮目录未显示可用的 PySCF/GPU4PySCF，不能声称它们已能替代现有量化引擎。

Phase19 新闭环求解使用已有 SciPy；其改善与新增数据集无关。
本轮不修改有效长计算的环境或源码快照，不重启已在运行的 Phase7/14/16。
