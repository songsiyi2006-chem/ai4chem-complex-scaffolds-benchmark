# Phase 1–19 显性问题收尾清单

本清单区分软件修复、完整计算和科学验收。**目前并未全部完成。**
计算记录以 [本轮状态](PHASE1_19_RERUN_STATUS.md) 和对应原始日志为准。
本文件不宣称实验验证，不把非收敛、采样不足或无合格候选改写为通过。

## 收尾边界

- 已通过所列检查的阶段冻结，除非出现明确的新反证，不重复重跑或扩展优化。
- 优先修复崩溃、错误数值/单位、残缺结果误判完成、旧结果混入和不可追溯的恢复。
- 不缩短生产轨迹、减少既定训练/扫描任务或放松收敛门槛来获得“通过”。
- 代码测试只证明对应软件行为，不替代真实引擎运行和科学验收。
- 科学指标未通过但结果计算真实、失败原因明确时，保留负结果；这不是目标性能达成。

## 按阶段的未结事项

| Phase | 当前处置 | 仍需完成的检查/动作 |
|---|---|---|
| 1 | 本轮所列检查通过，冻结 | 无新增阻塞；保留分子适用性局限 |
| 2 | 本轮所列检查通过，冻结 | 无新增阻塞；保留替代力场/电荷方法说明 |
| 3 | 完整性门槛、二进制检查点和失败退出修复 | 新的默认完整 MD、MM-GBSA 与图表验收；旧27帧不可续接 |
| 4 | XYZ 导出缺陷修复，已完成9张路径图像的真实力复算 | 独立投影/数值记录一致，残余力0.15066261仍高于0.05；真实未收敛，不得声称 TS/IRC |
| 5 | A/B/C 已计算，绘图已独立恢复 | 多虚频结构不能当作合格 TS；近零产率不代表成功催化 |
| 6 | 默认轨迹已完成 | 产物采样不足，势垒不可确定；不能用软件测试填补采样 |
| 7 | 原有效计算继续 | 全点完成后使用统一基组过滤的新合并器复核；回退点单列 |
| 8 | CLI、xTB/Psi4 参数、检查点/旧结果污染修复 | 已排队；完整 QC、动力学、NTO 和图表仍需真实运行 |
| 9 | 本轮所列检查通过，冻结 | 模拟与协议模拟器通过，不等于真实硬件验证 |
| 10 | 本轮所列检查通过，冻结 | 保留控制模型/模拟环境局限 |
| 11 | 完整计算结束 | He 误差未满足原精度目标，保留部分验收，不能称全部达标 |
| 12 | 默认训练检查点已修复 | 排队重跑完整训练并验收；旧无权重运行不可续接 |
| 13 | 排队 | 完整默认计算与结果验收 |
| 14 | 修正版完整运行中 | 等待全部子实验，检查统计与守恒结果 |
| 15 | 固定采样及检查点已修复 | 排队完整生产与窗口统计；旧残缺轨迹不算完成 |
| 16 | 默认完整运行中 | 水阶段后其余模块；保留未收敛/边界约定等科学局限 |
| 17 | 本轮指定检查及后审计通过，冻结 | 保留有限基组/原子叠加模型局限，非全配合物验证 |
| 18 | 本轮所列数值检查通过，冻结 | 保留热力学预测误差和非实验验证说明 |
| 19 | 碰撞计分/打包参考修复，新增骨架构造拒绝检查 | 尚无完整合格骨架；闭环肽链构造问题不能仅靠放宽静态门槛解决 |

## 本次集中软件核验

`run_phase1_19_regressions.py --scientific-profile molecular` 包含新增的
Phase3 检查点/退出状态、Phase4 NEB 诊断、Phase8 预检查和 Phase19 碰撞测试。
执行次数、跳过项和源码哈希以公开证据中的实际回归记录为准。
不将各次重复运行的测试数累加为独立测试数。

## 一次性计算安排

原有限队列保持 `16 → 12 → 13 → 15 → 8`，Phase7 和14的有效长计算不重启。
Phase3 使用 `prepare_phase3_restart.py` 准备同批前置结果的独立新目录；
`run_prepared_phase3.py <prepared-directory> --after-queue <queue_status.json>`
等待该队列正常结束且可用内存至少3.5 GiB后，执行默认完整 MD。等待不计为正在重算。
源文件和前置输入哈希不匹配、已有同阶段运行或队列异常时明确停止，不覆盖旧结果。
这是一条无周期触发器的单次任务，不是定期 AI 接续；脚本不自动修代码或推送。

Phase3 的精确二进制状态已保存，但 DCD 追加与累计分析恢复仍明确禁用；
检查点完整性验证并不等于已经实现可用的生产续跑。
