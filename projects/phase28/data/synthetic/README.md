# Synthetic benchmark isolation

The generating equations and seed are frozen in `../../configs/local.json`. Generated batch observations, latent ground truth, scenario schedules and uncertainty draws live in `../../results/run_20260920/`, with file names/evidence columns declaring their synthetic origin. None is a literature measurement, real electrode performance label or input to a publication-performance ranking. The local runner never fits model parameters to published traces.
