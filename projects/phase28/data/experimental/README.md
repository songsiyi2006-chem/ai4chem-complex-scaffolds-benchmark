# No new experimental data

`EXPERIMENT_REQUIRED / NOT_RUN`. The user limited this execution to public data and local computation. Published experimental source data are separately attributed under `../literature/`; they are not experiments performed by this repository.
