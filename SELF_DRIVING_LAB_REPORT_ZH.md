# 自驱动实验室报告 — 第九阶段
## 自驱动实验室编译器：Opentrons OT-2 硬件执行与贝叶斯闭环分析数字孪生

*流水线：`run_phase9_self_driving_lab_compiler.py` · 结果：`results_phase9/phase9_results.json` · 图表：`figures_phase9/`（300 DPI）*

---

## 摘要

本项目的第一至八阶段完全在硅基（in silico）世界中运行：构象、分子动力学、过渡态、微动力学、元动力学与多参考墙，全部都是**关于**化学的计算。第九阶段完成了向化学**本身**的范式跃迁：第四/五阶段合成的世界模型被编译为 Opentrons OT-2 移液工作站的机器可执行控制代码，由安全约束下的多目标贝叶斯主动学习进行优化，并通过自动化在线 HPLC 峰解卷积智能体闭环——读取色谱图、算出产率与对映体过量、回填优化器的，不是人类，而是算法。整场战役在第五阶段的旗舰反应（不对称氮丙啶扩环反应，催化剂为第五阶段逆向设计的 3,3′-CF₃-Ph/iPr-Ph-BINOL 膦酸）上执行了 **8 轮主动学习、共 50 次机器人实验**，最终收敛至 **91.9 % 产率 / 79.9–84.5 % ee**——恰好落在第五阶段微动力学世界模型在机器人启动之前预测的 89–97 % / 80–91 % 区间之内。编译产物通过**真实的 `opentrons_simulate` 引擎验证（rc = 0）**，内置的"幻觉审计"将分析孪生的测量误差限定在全部 50 次实验的 **|ΔY| ≤ 2.8 %、|Δee| ≤ 5.6 %**。

**核心数字：朴素四维设计空间中有 83.5 %（131,072 个采样候选中的 109,427 个）至少违反一条物理安全护栏——而受约束的优化器依然找到了无约束世界模型所预言的最优点，它是绕开了危险区，而不是穿过了它。**

---

## 1. 从干实验（dry-lab）仿真到具身智能（Embodied AI）

一个终止于硅基预测的化学世界模型只是假设生成器，不是化学家。第四至八阶段给出了反应网络、自由能面与设计催化剂——它们都是**条件式断言**："如果你跑这个实验，自然会如何回应"。第九阶段安装了科学闭环中缺失的另一半：

```
第四/五阶段世界模型 ──► 模块 9A ──► OT-2 机器人代码 ──► （虚拟）物理执行
       ▲                                                    │
       │                                                    ▼
  模块 9B  ◄── 解卷积后的 Y / ee / E ◄── 模块 9C ◄── 在线 HPLC A(t, λ)
  安全约束的          自动化智能体            EMG 色谱峰、
  q-EI 贝叶斯环路     （人在环外）            基线漂移、噪声
```

三重认识论转变支撑着这一架构：

1. **预测变得可证伪。** 从未接触过移液枪的微动力学产率预测只是修辞。一旦它被编译成孔位图、温度设定点与固定反应时间，它就能以"宇宙可以投票"的方式犯错。
2. **错误开始有价格。** 在仿真里，错误的垒值只损失一张图；在自驱动实验室（SDL）里，它损失试剂、机时——若安全建模失误，甚至损失硬件。因此优化器必须把**可行性视为硬性、不可协商的边界**，而不是罚项。
3. **测量层本身就是一个推断问题。** 真实探测器不会输出真值。闭环只有在自动化智能体能把漂移、含噪的原始色谱解卷积为*带量化误差*的转化率、产率与 ee 时才能成立——这正是模块 9C 自带幻觉审计、而不向优化器泄漏仿真真值的原因。

---

## 2. 模块 9A — 硬件协议编译器

### 2.1 目标平台与台面架构

目标平台：**Opentrons OT-2**，`opentrons.protocol_api` **v2.15** 语法。编译后台面布局（图 1）：

| 槽位 | 耗材 | 角色 |
|---|---|---|
| 1 / 2 | Opentrons 96 孔滤芯_tip 架（300 µL / 20 µL） | P300（左）/ P20（右） |
| 3 | USA Scientific 12 排储液槽（22 mL） | DCM、甲苯、HPLC 稀释液、MeOH 淬灭、EtOH 涮洗 |
| 4 | USA Scientific 12 排储液槽 | 底物储备液（0.50 M）、催化剂储备液（25 mM）、内标（200 mM TMB/DMSO） |
| 7 | **温控模块 GEN2** + 96 孔 200 µL PCR 板 | 反应板，T ∈ **[4, 95] °C**（战役使用 25–90 °C） |
| 9 | 铝块 + 1.5 mL 冻存管 | 密封的 CPA 催化剂母液 |
| 11 | 24 管 Eppendorf 架 | HPLC 进样瓶——每实验 2 瓶（两级稀释 1:40 → 1:20 = **1:800**） |
| 12 | 固定废弃物位 | tip 废弃 |

一个战役轮 = 一次协议运行 = 5 个 q-EI 条件（该轮柱的 A1–E1 孔）+ 12 个 HPLC 瓶；轮间更换耗材架。第二个产物 `output_ot2_protocol_champion.py` 编译最终帕累托代表性的冠军批次。

### 2.2 非水有机溶剂的液体类（liquid class）物理

编译器的核心现实主义主张是：**机器人移取的不是"液体"，而是"液体类"**。引擎内置六个标定类（见图 1 表格）：

- **DCM**（ρ 1.326，η 0.43 mPa·s，20 °C 蒸气压 473 mbar——*易挥发*）：吸液 35 µL/s，排液 50 µL/s，6 mm 深 tip 浸没、**即时排液**、2× 预润湿——防范的正是蒸汽锁。
- **甲苯**（参考有机类）：70/90 µL/s。
- **DMSO**（内标载体，*高黏*，η 1.99）：12/18 µL/s，3× 预润湿，最慢流速。
- **MeOH 淬灭**与 **EtOH 涮洗**：55/75 µL/s；EtOH 用于有机相之间的 tip 养护。
- **HPLC 稀释液**（MeCN + 0.1 % FA + 内标）：80/100 µL/s，分析类。

每个类均以 **10 µL 空气间隙**、`well.top(−2)` 处 **blow-out**、**touch-tip** 挂壁完成转移——防滴三件套。体积由编译器按仪器路由：≥ 30 µL → P300；5–20 µL → P20；低于 5 µL 的 blend 组分被编译为*显式注记的剂量偏差*，而非默默低于移液最小量执行。

### 2.3 多步合成脚本

每个条件块被发射为物理时序的机器人代码：`set_temperature(T)` → `await_temperature(T)` → 溶剂混合（φ-甲苯/DCM 拆分）→ 40 µL 底物 → 催化剂剂量（25 mM 下 4·mol% µL）→ P20 混匀（8 × 18 µL）→ 反应保温 `ctx.delay(minutes = 60·t)` → 15 µL MeOH + 5 µL 内标淬灭 → **两级系列稀释**（5+195 µL，再 10+190 µL → 1:800）进入 HPLC 瓶 → EtOH 涮洗。一个完整的 5 条件轮编译为约 300 行协议代码：79 次吸/排液、45 次空气间隙、44–63 小时的顺序热调度。

### 2.4 AutoProtocol（JSON-LD）云实验室导出

`results_phase9/autoprotocol_workflow.jsonld` 以 AutoProtocol 方言的 JSON-LD 序列化同一工作流（`@vocab` 为 autoprotocol.org，Strateos 兼容的 `refs`/`steps`：15 条转移 + 保温 + 淬灭/系列稀释块 + 以机器可读元数据形式附加的三条安全护栏）——面向 Emerald Cloud Lab / Strateos 类接收方的互操作提交格式。

### 2.5 验证：三层关卡与一段诚实的环境事故记录

`results_phase9/ot2_validation_report.json` 完整记录了验证关卡：

1. **字节编译** —— 两个产物 `py_compile` 均 PASS。
2. **AST 审计** —— 存在 `run(ctx)` 入口；温度落于 [4, 95] °C；无文件 I/O / exec；以及感知控制流的 **tip 纪律检查**（每次吸/排液都在已夹 tip 状态下发生；夹取数 = 归还数）。
3. **真实协议仿真** —— 在隔离 venv 中对两个编译产物运行 `opentrons_simulate`：第一轮与冠军批次均 **PASS，rc = 0**。

两条环境经验被原样保存，因为它们属于"人人都会踩、无人写进文档"的类型：**opentrons 9.1.2 默默放弃了对 OT-2 的支持**（拒收 OT-2 协议并引导用户转向 Flex——OT-2 仿真必须用 **8.3.0** 系列）；而第一次 venv 尝试在 Python 3.14 上从源码以实验性 MINGW-W64 工具链编译 **numpy 1.26.4**（3.14 无官方 1.26 轮子），导致非确定性段错误。改用 Python 3.12 解释器重建 venv（官方 numpy 轮子）后，仿真器变为确定性。验证器因此对原生崩溃采用重试并标记*不确定*（回退到对完整 API 表面执行协议、记录调用轨迹的 mock 沙盒），确保关卡永远不会把"宿主机坏了"误判为"协议坏了"。

---

## 3. 模块 9B — 安全约束的多目标贝叶斯主动学习

### 3.1 目标

| 目标 | 定义 | 实测最优 |
|---|---|---|
| 最大化产率 | Y ∈ [0, 100] %（HPLC） | **91.9 %**（R7-5） |
| 最小化 E-因子 | E = 废弃物质量 / 产物质量（200 µL 孔基准） | **93.1 kg/kg**（R5-1） |
| 最小化催化剂成本 | (mol%/100)·$480/mol ÷ 产率 | **$5.7 /mol**（R5-1） |

微孔尺度的 E-因子（65–1373）是孔级质量衡算的真实结果而非缺陷：200 µL 规模的发现化学，其 E-因子天然比工厂规模差一个数量级——这正是 E-因子必须作为优化**目标**而非事后补充的原因。

### 3.2 不可协商的物理护栏

三条解析约束函数对每个候选做**精确**评估（安全绝不使用学习代理）：

- **G1 放热** —— ΔT_ad = ΔH_eff·C / (ρ·Cp) < **30 K**：0.1 M 孔基、Assigned ΔH_eff = 72 kJ/mol（开环 + 中和，透明账本登记）、DCM/甲苯混合 ρ·Cp。富 DCM 孔 ΔT_ad ≈ 45 K，直接拒绝。
- **蒸气压** —— T_反应 < T_沸(φ) − **15 °C**：T_沸为混合物的 **Antoine/Raoult 泡点**（DCM 沸点 39.6 °C → 纯 DCM 将 T 限制在 24.6 °C；纯甲苯允许 95.6 °C）。这是占主导的约束。
- **黏度/压降** —— 微流控进样 ΔP = 8ηLQ/πr⁴ < **15 bar**（50 µm 内径、30 cm、0.10 mL/min，对数线性黏度混合 + Arrhenius 温度修正）。

整个战役的采集网格上，**131,072 个候选中 109,427 个（83.5 %）**违反至少一条护栏——G2 沸点 94,502（72.1 %）、G3 黏度 9,653（7.4 %）、G1 放热 5,272（4.0 %）。可行岛位于富甲苯、中温区；而*化学本身*在低 φ 处想要的恰恰是 G2 禁止的区域——约束真正地雕琢了搜索空间。

### 3.3 真值"物理"反应器

随机孪生逐参数锚定于第五阶段计算量：Eyring 速率作用于 **21.24 kcal/mol** 的 RDS 垒（模块 A 的 TS1），叠加 Assigned 的 ±1.30 kcal/mol 极性抛物线；**1.50 kcal/mol** 的立体分化给出 ee = tanh(ΔΔG‡/2RT)——25 °C 时 85.2 %，与模块 D 的 85.27 % 参考值吻合；27 kcal/mol 的消旋背景通道在低负载时稀释 ee；温度门控的消除/聚合副反应通道复现第五阶段网络的离轨路径；孔级质量衡算把一切换算为 E-因子与成本。所有 Assigned 常数逐条列于 JSON 的 `surrogate_ledger_assigned` 块——零隐式参数。

### 3.4 采集与闭环结果

采集器：对*测量值*（绝非真值）拟合每个目标的 **Matérn-5/2 + White 高斯过程**（`scikit-learn`）→ 每批成员随机 Dirichlet 权重的 minimax-Tchebycheff 标量化 → 在 16,384 点 Sobol 网格上做**局部惩罚期望改进**（Ginsbourger 式 batch-q-EI），并以解析护栏掩罩；q = 5，8 轮，10 个修复后的 Sobol 初始点（不可行初始点的投影被记录、绝不执行——**50/50 的已执行实验全部满足护栏**）。

| 轮次 | 里程碑 | 实测结果 |
|---|---|---|
| 0（Sobol） | INIT-02 | 91.9 % Y，**84.5 % ee**，E 251 |
| 2 | R2-5 | 85.9 % Y，83.6 % ee，E 105 |
| 5 | R5-1 | 87.9 % Y，E **93.1**，**$5.7/mol**（成本/E 拐点） |
| 7 | R7-1…R7-5 | **Y 89.4–91.9 %，ee 76.5–83.5 %**——设计催化剂区 |
| 8 | R8-2 / R8-3 | 91.2 / 88.4 % Y，催化剂成本仅为 R7-5 的一半 |

可行前沿的三维超体积：**0.815 → 0.848**（+4.0 %），自第 4 轮起平台化——主动学习收敛的标志（图 2）。最终帕累托集含 7 点，从 $5.7/mol 的绿色拐角（R5-1：87.9 % Y，E 93）延伸到 91.9 % 产率拐角（R7-5），四点冠军批次被编译进冠军 OT-2 协议。

**与世界模型的一致性**：战役最优（T ≈ 36–40 °C、6–10 mol% 设计 CPA、约 23 h、富甲苯）给出 Y = 91.9 %、ee = 79.9–84.5 %——落在第五阶段对同一催化剂预测的 **89–97 % 产率、80–91 % ee** 包络内。物理反馈层没有推翻硅基层，而是*确认*了它——这是认识论上平淡却至关重要的结果。

---

## 4. 模块 9C — 在线分析遥测与解卷积智能体

### 4.1 正向探测器模型

每次执行的实验都会产生多波长检测器输出 A(t, λ)（**210 / 254 / 280 nm**）：七个色谱组分（内标、催化剂、底物、手性柱上的**产物 R / 产物 S**、消除与聚合副产物）各有摩尔吸光系数；**指数修正高斯（EMG）**峰形（保面积参数化，σ 0.06–0.20 min、τ 0.12–1.0 min、浓度依赖拖尾）；0.42 min 死时间；经 1:800 稀释与进样塞宽度标定的 Beer–Lambert 面积；基线偏移/斜率/摆动漂移与 0.4 mAU 探测器噪声。

### 4.2 自动化智能体（没有任何人类阅读色谱图）

每个实验依次执行：**非对称最小二乘基线校正**（Eilers–Boelens）→ 显著度峰检测 → 窗口化**有界多 EMG 最小二乘拟合**（soft-L1 损失）→ 按期望表观保留（tR + t0）指认组分 → 内标归一定量。输出转化率、产率、ee（由基线分辨的 R/S 面积对计算）、副产物面积分数、拟合残差与完整拟合参数集——图 3 渲染的正是这些：5 轮的原始 vs 解卷积色谱，副产物窗口随学习者抛弃高温富 DCM 条件而收缩：**6.8 → 5.5 → 4.9 → 4.3 %**。

### 4.3 幻觉审计

优化器只见*测量值*；仿真真值仅留作审计。全部 50 次实验：**平均 ΔY = −1.4 %**（最大 |ΔY| = 2.8 %），**平均 Δee = +1.7 %**（最大 |Δee| = 5.6 %）。残余偏差是诚实的分析行为（回收率 ≈ 98.6 %；手性拖尾扰动小组分对映体），并且*逐实验*记录在主 JSON 中，而非隐藏。这就是 §1 认识论主张的具体实现：孪生体被构造成"AI 内部模型与测量层可以不一致"，而不一致本身是一等科学对象。

---

## 5. 作为幻觉过滤器的物理反馈回路

LLM 时代的化学有一种特定失败模式：流畅而不可证伪的操作叙述。第九阶段架构是三层解药：

1. **编译即证伪。** 一份（无论 LLM 还是模型写的）配方，要么能编译为体积可行、tip 合法、热安全的机器人代码——要么不能。开发过程中 AST 审计就抓到一个真实缺陷：一次未夹 tip 的 `mix()` 调用，在实体 OT-2 上会让反应中途停机。
2. **护栏由构造而不可协商。** 83.5 % 的朴素设计空间不安全；任何幻觉出的"DCM 里高温猛跑"条件都会在排程*之前*被解析地（而非概率地）拒绝。
3. **测量闭环闭合于模型而非人类。** 贝叶斯优化永远见不到仿真真值——它拟合的是 HPLC 解卷积量，而这些量对真值的误差本身有界且已归档。未来把真值反应器换成真实探测器，优化器代码零改动：模块间契约是 JSON schema，审计表就是收据。

---

## 6. 路线图：迈向自主 AI 化学家

| 阶梯 | 状态 | 下一步 |
|---|---|---|
| 结构与能量（第 1–3、6–7 阶段） | 完成 | — |
| 反应网络与微动力学（第 4–5 阶段） | 完成 | — |
| 生成式催化剂设计（第 5C） | 完成 | 让第 9 阶段闭环*物理地*闭环（催化剂结构的贝叶斯优化） |
| 配方 → 机器人编译（9A） | **完成** | Opentrons Flex 迁移、在线 IR/MS、手套箱液体类 |
| 安全约束自主（9B） | **完成** | BoTorch 的约束 EHVI/ParEGO；对约束边界本身的主动学习 |
| 分析自主（9C） | **完成** | 真实 HPLC/UV 守护进程、NMR 层级贝叶斯解卷积、不确定性感知定量 |
| 完全自主 | — | LLM 规划器统筹 9A–9C 并记忆历史战役；人类角色收敛于补货与会签安全包络 |

值得坚持的架构不变式：**世界模型提出，编译器裁决，探测器验证**——三个可独立证伪的层级，以 schema 相连，而非以信任相连。

---

## 7. 复现

```bash
python run_phase9_self_driving_lab_compiler.py            # 完整 8 轮战役
python run_phase9_self_driving_lab_compiler.py --selftest # 3 轮冒烟测试
python run_phase9_self_driving_lab_compiler.py --fig_only # 重生成 300 DPI 图表

# 真实硬件语法关卡（隔离 venv；版本约束见 §2.5）
C:/Users/HUIWEI/miniconda3/envs/phase2ff/python.exe -m venv .ot2env
.ot2env/Scripts/python.exe -m pip install "opentrons==8.3.0"   # 9.x 已放弃 OT-2
.ot2env/Scripts/opentrons_simulate.exe output_ot2_protocol.py  # -> rc 0
```

产物：`output_ot2_protocol.py`（根目录，第一轮批次）、`results_phase9/output_ot2_protocol_champion.py`、`results_phase9/autoprotocol_workflow.jsonld`、`results_phase9/phase9_results.json`、`results_phase9/ot2_validation_report.json`、`figures_phase9/fig1–fig3`（300 DPI）。

## 8. 透明账本

第五阶段继承量（垒值、立体分化、参考产率/ee）均为*计算值*，按引用携带。第九阶段的战役标定常数——溶剂垒抛物线（±1.30 kcal/mol）、背景垒（27 kcal/mol）、副通道动力学（0.035、32 K、0.55）、有效放热（72 kJ/mol）、催化剂价格（$480/mmol）、HPLC 标定（ε 表、W = 0.20 min）、噪声底——均以 `assigned` 标记于 `results_phase9/phase9_results.json`。零静默回退；验证器完整记录 OT-2 环境事故（§2.5），而不是只报告一个光秃秃的 "PASS"。

---

*图表：[`fig1_robotic_deck_architecture.png`](./figures_phase9/fig1_robotic_deck_architecture.png) · [`fig2_bayesian_pareto_frontier.png`](./figures_phase9/fig2_bayesian_pareto_frontier.png) · [`fig3_inline_hplc_deconvolution.png`](./figures_phase9/fig3_inline_hplc_deconvolution.png)*
