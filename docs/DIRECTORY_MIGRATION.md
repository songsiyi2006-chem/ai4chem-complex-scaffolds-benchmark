# 分阶段目录迁移记录

本次将散落在根目录的阶段文件实际归档，而非仅增加导航。

## 文件归属

- `projects/phase01` 至 `projects/phase27`：每阶段独立入口，集中代码、报告、结果、图表与专属测试。
- `shared/audit`：跨阶段检查、队列与审计证据；原始源码快照留在证据目录。
- `shared/phase25_27`：三个催化阶段共用的输入、分析、来源和联合验收。阶段 25、26 的专属模块已移至各自代码目录；阶段 27 原本没有生产动力学引擎，未凭空添加。
- `projects/legacy_solubility`：保留历史辅助脚本，与编号阶段分开。
- `tools/run_phase.py` 与各阶段 `run.py`：在独立目录还原原执行布局，不覆盖仓库数据。

## 保留与验证

迁移基线提交：`15efde6d29fbf81e3b1a1876734ed9d45e693979`。
[逐文件清单](layout_manifest.json)记录 1,936 个原文件，其中 1,929 个改变位置。
全部原文件都有唯一新路径；1,839 个非 Markdown、非根配置文件保持 SHA-256 不变，其中包括全部原 Python 源文件和原始科学数据。

可点击 Markdown 链接已按新位置调整；原始文档字节压缩保留，准备运行工作目录时可以还原，以继续核对原有证据哈希。技术叙述中的旧命令和路径仍指原运行布局，使用方法见[复现指南](REPRODUCE.md)。

已执行的迁移验证：

| 范围 | 结果 |
|---|---|
| 文件覆盖、27 个阶段入口、源码解析与 Markdown 路径 | `tools/validate_layout.py` 通过；161 个原 Python 文件可解析 |
| 独立工作目录、跨阶段/包导入、输出隔离、拒绝覆盖与来源变化 | 6 个迁移测试通过 |
| Phase 23 模型软件自检，经新阶段入口执行 | 10 个测试通过 |
| 重建后的 `phase25_27.test_analysis` | 18 个测试通过 |
| 原 campaign 测试与 `phase25_27.test_pilot` | 13 个测试通过 |

总计 47 个软件测试通过。科学依赖复用本机已有环境；Windows 初次仅指定解释器未加载 Conda DLL 路径，使用正确环境路径后复核通过。未安装或升级软件。

没有执行全部阶段生产计算，也没有将目录检查、源码完整性或软件测试解释为新的科学验收。

```sh
python tools/validate_layout.py
python -m unittest discover -s tools -p test_layout.py -v
```
