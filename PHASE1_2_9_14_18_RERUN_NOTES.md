# Phase 1 / 2 / 9 / 14 / 18 本轮修复

本轮采用新目录实际计算，保留源代码 SHA256、运行日志与结果。
状态以 `PHASE1_19_RERUN_STATUS.md` 为准；本文件不是全流程通过声明。

- Phase 1：报告标题按实际注册条目数生成；构象栏区分接受数与嵌入数，不把未收敛构象算作有效结果。首次默认计算 130.4 秒：10 个有效条目各接受 50 个构象；故意保留的无效 M09 被解析器拒绝，M09R 替代条目通过。报告修正版再次新算。
- Phase 2：扭转扫描允许最多 6 次延续优化，每次 800 步，收敛与角度阈值不放宽；任一扫描失败使最终退出码非零。
- Phase 9：官方 Opentrons 验证失败不会被 mock 通过覆盖；支持显式指定模拟器。仅官方模拟成功且编译/AST 检查成功才设置 `hardware_validation_passed`，该字段不代表湿实验安全认证。
- Phase 9：25 mM 催化剂对 20 μmol 底物的加样系数应为 8 μL/(mol%)，原 4 的系数只给足一半剂量；协议、JSON-LD 和反应体积同步修正。催化剂载体溶剂计入质量账。
- Phase 9：μL × mM × g/mol 换算 mg 的系数应为 1e-6；$/mmol 换算 $/mol 应乘 1000，并同步成本归一化区间。环扩张模型底物/产物采用同一 C10H11N 分子量。
- Phase 9：竞争的催化与背景通道共享同一底物池，改用总一级速率和分支比，确保转化之和不超过 1；EI 使用同一权重下、实际可行观测的标量化最优值，不能用各目标独立最小值的均值充当基准。该采集仍是局部惩罚的近似批量 EI，不是精确联合 q-EI。
- Phase 9：二氯甲烷液体比热修正为 102.3/84.93≈1.2045 J/(g·K)，原 0.120 错一个数量级。来源：[NIST Chemistry WebBook](https://webbook.nist.gov/cgi/cbook.cgi?ID=C75092&Mask=2&Units=SI)。常温常比热近似不等于全温区实验校准。
- Phase 9：明确标记全部反应/HPLC 为模拟；所谓 Phase5 锚点是旧版赋值参数，本轮并未读取 Phase5 输出，不能声称是已验证跨阶段计算或真实机器人实验。
- Phase 14：FRAP 共轭梯度失败时，不再静默删掉扩散项。对同一方程增加迭代重试，并检查线性残差；仍失败则显式报错。
- Phase 14：追加输入浓度和时间步的有限性检查，拒绝 NaN/Inf 以及不能推进时间的步长；新候选场出现非有限值时不允许更新状态。四项针对性测试通过，覆盖总量守恒、自由能曲率、无效状态拒绝和 FRAP 两次求解失败后的显式报错。正在运行的早期快照不包含这些后加的防御检查，需独立审阅其实际输出有限性。
- Phase 14：修复终点步长上限未传入实际积分，以及提前将旧场存成未来时刻快照的问题。现在实际到达边界后才保存快照，第五项确定性测试通过。当前早期全量尝试仍需注明快照标签存在最多一个建议时间步的提前量、终点可能稍有超出；不能称为精确时刻输出，此项修正版尚未完成全量重算。
- Phase 18：MILP 重试更改全局热力学 epsilon 时使用 try/finally 恢复，即便异常也不污染后续计算；耗时包含重试。

回归入口：`python -m unittest -v test_phase1_19_campaign test_phase1_5_audit test_phase6_10_audit test_phase14_17_audit test_phase18_19_audit`。
完整计算输出不等于科学接受：特别要检查结构优化、SCF、动力学积分、守恒与替代模型标记。
