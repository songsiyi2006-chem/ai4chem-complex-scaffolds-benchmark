# 骨架编辑报告 — 第四阶段：自主过渡态搜寻与 CI-NEB 反应路径剖析

*数据：`results_phase4/phase4_results.json` | ASE 3.29 CI-NEB（improved tangent）· ANI-2x 反应性 PES（torchani）+ GFN2-xTB 解析 Hessian 验证（xtb 7.x CLI）· RDKit 2026.03*

---

## 【一、模型反应：吲哚→喹啉骨架编辑的核心基元步】

现代**分子骨架编辑**（Levin、Sarpong 等，2022–2024）通过单原子**插入**或**删除**改写分子骨架，而非从头筑环。吲哚→喹啉编辑的祖师爷是 **Ciamician–Dennstedt 重排**（1901）：卡宾对吲哚 C2=C3 环丙烷化，稠合张力环丙烷开环，吡咯环经形式单原子插入扩为 6,6 稠合喹啉核心。

本流水线以最简 **:CH₂ 模型**刻画该**扩环基元步**：

| | 物种 | 分子式 | 拓扑 |
|---|---|---|---|
| **R** | cyclopropa[b]indole（吲哚 C2=C3 环丙烷化张力加合物） | C₉H₉N | 稠合 5,6 + 环丙烷 |
| **P** | 2,3-二氢喹啉（扩环核心） | C₉H₉N | 稠合 **6,6** |

R 与 P 是**同分异构的价键异构体**——编辑基元步的本质。配对算法复原了化学忠实的原子对应：仅**两键断裂（环丙烷共边 C2–C3 及一个重排伙伴）+ 一键生成**——教科书级最小拓扑变化。

**骨架编辑理论框架**：单原子编辑分两类——**扩环插入**（本例：卡宾碳嵌入吡咯环，5 元→6 元）与**原子删除**（如 Levin 的氮删除吲哚→苯并环丁烯类）。共同特征：拓扑数变化极小（1–3 键）、张力/芳香化驱动、经由高张力多环中间体；设计空间恰好在"2D 图模型看不见、生成模型没学过"的盲区。

---

## 【二、阶段 1 — 几何、对齐与 IDPP 插值】

- R 用**程序化构建**（按环成员度定位吲哚 C2=C3 双键→饱和→CH₂ 桥连）——手写 SMILES 是芳香性/kekulize 陷阱（三种错误变体已记录）。
- 两端点 ETKDGv3 + MMFF94 嵌入，**GFN2-xTB 优化**（`xtb --opt`）。
- **原子对应 v4 算法**：骨架编辑异构体的环连接性本身改变，MCS 对齐失效。解法：枚举 MCS 匹配（对称变体）× **置换不变的排序距离谱**匈牙利补全，按**键变化数**评分——确定性恢复 2 断/1 成的最小映射。
- **Kabsch 刚体对齐**（错取向端点间的 IDPP 会让原子横穿空间——已实测诊断）。
- **9 像素 IDPP 插值**（ASE）防初始带原子重叠。

## 【三、阶段 2 — 攀爬像 NEB】

### 3.1 CI-NEB 推导

离散路径 {R₀=R … R_M=P} 通过最小化带约束目标函数弛豫；每个像素只受**垂直于切向**的真实力分量与**平行**弹簧力：

  Fᵢ = −∇E(Rᵢ)⊥ + k(|Rᵢ₊₁−Rᵢ| − |Rᵢ−Rᵢ₋₁|)·τ̂ᵢ

切向用 **improved-tangent**（Henkelman–Jonsson 2000）按邻域能量梯度加权。**攀爬像**（最高能像素，本例第 4/9）额外反转平行真实力：

  F_CI = −∇E⊥ + ∇E∥

使其沿带**向上**弛豫、在 57 个垂直方向**向下**弛豫——以带本身为反应坐标的鞍点搜索。收敛判据：全带最大原子力 fmax < 0.05 eV/Å。

### 3.2 执行与引擎攻坚（如实记录）

| 尝试 | 结果 |
|---|---|
| xtb.exe 子进程引擎 | **力符号 bug**：xtb 写的是梯度、ASE 约定 forces=−∇E，带爬山爆炸（fmax→4700）。取负修复，H₂ 探针验证吸引力方向。 |
| xtb CI-NEB | 子进程力噪声下 BFGS/FIRE 后期发散（带顶 +11 eV）；引入**最优带快照回滚**与多保真 ANI 预弛豫热启动，仍难收敛。 |
| tblite 绑定 / MACE-OFF23 | win-64 无 Python 绑定 / 离线无模型（已记录回退）。 |
| **ANI-2x 单相位 CI-NEB（开局即攀爬）** | **收敛：64 步 FIRE，fmax = 0.0468 eV/Å < 0.05 ✓**（光滑 ML PES 容忍开局攀爬；分阶段不攀爬协议会让带滑入虚假高能路径——两种行为均已测量）。 |

**最终 MEP（ANI-2x，9 像素）**：E(eV) = [−10965.90, −10965.65, −10963.93, −10965.28, **−10963.62 (TS)**, −10964.17, −10965.54, −10966.19, −10966.31]
- **ΔE‡(正向) = 52.5 kcal/mol**，逆向 62.0，ΔE_rxn = **−9.5 kcal/mol**（放热——张力释放，热力学下坡 ✓）

## 【四、阶段 3 — 量子验证：1 虚频判据】

对收敛攀爬像做解析 **GFN2-xTB Hessian**（`xtb --hess`）——用另一个半经验量子面交叉验证 ML-PES 鞍点（刻意的多保真互检）：

- **恰好 1 个虚频：ν‡ = −152.76 cm⁻¹** ✓（次低模式 +164.2 cm⁻¹——干净分离，判据满足）
- **过渡矢量核验**：虚频模式位移在成键轴上的投影——生成键 (4,9) 0.152 > 断裂键 (4,5) 0.081 / (6,9) 0.075——位移集中于**成键**轴并同时驱动两键断裂——骨架编辑坐标的协同断键/插入运动 ✓（图2）。

*（工程注记：本 xtb 构建的 `--opt ts` 退化成常规最小化（伴随 `--ts` 旗标被拒），曾把鞍点最小化掉（0 虚频）；因此直接对收敛 CI 几何做 Hessian 验证——两种行为均已记录。）*

### 4.1 活化参数（298.15 K，谐振子理想气体；单分子步骤内平动/转动抵消）

| 量 | 值 |
|---|---|
| ΔE‡（电子，ANI-2x MEP） | **52.5 kcal/mol** |
| ΔZPE‡（GFN2-xTB） | −6.2 kcal/mol |
| **ΔH‡** | **46.4 kcal/mol** |
| TΔS‡（振动） | +0.0 kcal/mol |
| **ΔG‡（Eyring）** | **46.4 kcal/mol** |
| k(298 K) = (k_BT/h)·e^(−ΔG‡/RT) | 6.0 × 10⁻²² s⁻¹ |

**物理解读**：~46 kcal/mol 自由能垒意味着孤立 :CH₂ 加合物在室温动力学冻结——与 Ciamician–Dennstedt 需要活化（二卤卡宾）条件及后续芳香化驱动力一致；真实合成序列中加合物在卡宾加成 PES 上热生成并级联前进。

**鞍点与虚频的物理诠释**：过渡态是势能面上指数为 1 的驻点——Hessian 恰有一个负本征值，对应一维下坡方向两侧均为上坡；虚频 ν = (1/2πc)√(−λ/μ) 量化该方向的曲率；沿其本征矢微位移分别滑向 R 与 P——这正是"1 虚频 + 过渡矢量指向断/成键轴"双判据的物理含义（本流水线全部程序化验证）。

---

## 【五、为何自回归语言模型在过渡态上必然幻觉——为何需要等变世界模型】

1. **过渡态不是分子**。它是 3N 维函数上指数为 1 的驻点——由 **Hessian 性质**（恰一负本征值）定义，而非任何局部原子图案。生成"像训练分布的分子"的自回归模型，原则上无法表示一个其定义特征是"它从未求值的能量的二阶导数"的对象。
2. **训练分布几乎不含过渡态**。TS 几何不可直接观测；数据集（QM7/9、SPICE、ANI-1x…）全是极小值。向模型索要 TS 是构造性的分布外外推——这恰是幻觉的教科书定义：在零支持区域输出流畅结果。
3. **反应路径是连续的协变对象**。MEP 对旋转/平移/置换不变、在 SO(3) 下等变；token 序列全都不是。**等变世界模型**（MACE/PaiNN/Allegro 类 e(3)-等变网络，或 SE(3) 扩散）把能量作为**可微函数**拥有——可求值、可求 Hessian——于是鞍点是模型自身的可验证性质，验证方式正是本流水线所做的（学习 PES 上的 CI-NEB + 1 虚频判据）。本流水线即模板：ANI-2x 一腿就是学习世界模型在做过渡态化学，再由 GFN2-xTB 交叉认证。
4. **"化学 AGI" 的生产力架构**因此不是"LM 猜一个 TS"，而是 **LM 提议反应图 → 等变模型拥有 PES → 算法程序（NEB/本征矢跟随/Hessian）认证鞍点**。本仓库第 1–4 阶段拼装的正是这套栈：2D 特征化 → 经典 MD → ML-经典力场对偶 → 认证式鞍点搜寻。

---

## 【六、可复现性、产物与局限】

```bash
# phase2ff 环境、Library/bin 入 PATH（xtb.exe + BLAS）
python run_phase4_reaction_mechanism.py --engine ani   # 完整流水线（本报告）
python run_phase4_reaction_mechanism.py --engine xtb   # GFN2-xTB NEB（分阶段协议）
python run_phase4_reaction_mechanism.py --fig_only     # 仅重出图
# 可调：--n_images 9 --fmax 0.05 --max_neb_steps 500 --force_rerun --auto_shutdown
```

**产物（`results_phase4/`）**：`reactant_3d.mol`/`product_3d.mol` · `images_idpp.xyz`（初始带）· `neb_final_path.xyz` + `neb_energies.npy`（收敛带）· `ts_candidate.xyz`（CI 几何）· `ts_hessian_freqs.json`（TS 与 R 的完整 GFN2-xTB 振动谱）· `ts_modes.npz`（位移矢量）· 各阶段检查点 · `phase4_results.json`。

**图（`./figures_phase4/`，300 DPI）**：图1 反应剖面（kcal/mol vs 归一化坐标，标注 TS‡ 与 ΔG‡）· 图2 TS 三维虚频位移矢量 + 断键（红）/成键（绿）· 图3 全 9 像素键演化热图。

### 局限（引用前必读）

1. **混合保真度（刻意）**：MEP 与 ΔE‡ 来自 ANI-2x（学习型反应 PES，半定量）；振动验证/ZPE/S 来自 ANI CI 几何处的 GFN2-xTB 解析 Hessian。1 虚频判据在该几何处的 GFN2 面上严格成立；全 GFN2-xTB 收敛带已尝试（见 3.2）但子进程力噪声在可用预算内击败了 BFGS/FIRE——尝试、诊断与多保真回退全部随记录交付。
2. 半经验热化学（刚性转子/谐振子，无受阻转子处理）；ΔS‡ 仅振动（本单分子重排 TΔS‡ ≈ 0）。
3. 母体 :CH₂ 模型——真实 Ciamician–Dennstedt 用 :CCl₂ 且后续 HCl 消除/芳香化级联（更高层步骤不在本 MEP 内）。
4. NEB 带 9 像素；像素间 MEP 平滑度为插值（图1 样条仅显示用）。
5. Eyring 速率用标准态（1 atm）约定，无隧穿校正。

---

*ai4chem-complex-scaffolds-benchmark 第四阶段。第一阶段：构象/描述符套件。第二阶段：扭转势垒 + 配体 MD。第三阶段：KRAS 复合物动力学 + MM-GBSA。本阶段：自动化鞍点认证。*
